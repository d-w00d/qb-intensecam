
--##-- add tables with appropiate gun names to blacklist within a vehicle --##--

-- Keep true if wanting to disable gun in vehicle

Config.BlackListedCarGuns = {
    --[`weapon_name`] = true,
}


--##-- Change these example hashes of cars and make sure set  = true and vehicle will --##--
-- Keep true if wanting to disableaim in car
Config.BlackListedCars = {
    --[model] = true,
}
