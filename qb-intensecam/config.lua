
--##-- add tables with appropiate gun names to blacklist within a vehicle --##--

-- true = enabled

Config.BlackListedCarGuns = {
    --[`weapon_name`] = true,
}


--##-- Change these example hashes of cars/Keep true if wanting to disableaim in car for certain cars --##--

Config.BlackListedCars = {
    --[model] = true,
}
